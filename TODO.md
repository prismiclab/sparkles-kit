# TODO — sparkles-kit

## Now

- [ ] `npm install` + `npm run build` to confirm the showcase compiles
- [ ] Decide which GitHub account/org the public repo lives under
- [ ] `gh repo create` + first push

## Soon

- [ ] Deploy showcase as `wyss.cx/sparkles/` (wyss-cx Astro config + redirect or subpath build)
- [ ] Add a real screenshot / OG image to README + meta tags
- [ ] Cursor-trail sparkle helper (the `.sk-sparkle-transient` class is in the CSS but no JS attaches it yet)
- [ ] Light theme variant — same primitives, lighter surfaces

## Later

- [ ] npm package (`@nicowyss/sparkles-kit`) so people can `npm i` instead of copy-paste
- [ ] React + Vue + Svelte component variants alongside the Astro ones
- [ ] Storybook / component playground
- [ ] Screenshot tests across browsers (`@property` Firefox baseline)

## Done

(Move items here when shipped, or rely on git history.)
