---
interface Props {
  as?: string;
  class?: string;
}

const { as = 'div', class: className = '' } = Astro.props;
const Tag = as;
---

<Tag class={`sk-reveal ${className}`.trim()}>
  <slot />
</Tag>
